<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" >
    <LinearLayout
        android:id="@+id/_textViewLabel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="10dp"
        android:gravity="center_vertical"
        android:orientation="horizontal" >
        <TextView
            android:id="@+id/textViewLabel"
            style="@style/JiraLabel"
            android:text="@string/description" />
        <View
            style="@style/Divider"
            android:layout_marginLeft="5dp" />
    </LinearLayout>
    <pl.edu.amu.wmi.betterjira.view.BJEditText
        android:id="@+id/bjeditTextDescription"
        style="@style/BJEditText"
        android:hint="@string/click_to_add_description"
        android:labelFor="@id/_textViewLabel" />
</LinearLayout>